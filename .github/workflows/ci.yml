# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
# This workflow will build a Java project with Gradle and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-gradle

name: Java CI with Gradle

on:
  push:
    branches: [ test-branch-for-ci-cd ]
  pull_request:
    branches: [ master ]

jobs:
  build:
# we can specify different Operating Systems to run our job using matrix
    runs-on: macos-latest
    # runs-on: ${{matrix.os}}
    # strategy:
      # matrix:
        # os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
    - uses: actions/checkout@v2

    - name: Set up JDK 8
      uses: actions/setup-java@v2
      with:
        java-version: '8'
        distribution: 'adopt'

    - name: Grant Executable Permissions for Gradle
      run: chmod a+x gradlew
        
    - name: Build with Gradle
      run: ./gradlew build

      uses: mr-smithers-excellent/docker-build-push@v5
      with:
        # image: docker-hub-repo/image-name
        image: manojchow72/my-private-repo/my-first-push
        registry: docker.io
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
 
      # uses: gradle/gradle-build-action@4137be6a8bf7d7133955359dbd952c0ca73b1021
      # with:
        # arguments: build
    # - name: Build and Push Docker Image
    #   uses: mr-smithers-excellent/docker-build-push@v4
    #   with:
    #     image: manojchow72/my-private-repo
    #     # tag: latest
    #     registry: docker.io
    #     username: ${{secrets.DOCKER_USERNAME}}
    #     password: ${{secrets.DOCKER_PASSWORD}}